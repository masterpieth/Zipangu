<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.syuusyoku.zipangu.dao.InterviewMapper">

	<select id="selectList" resultType="qvo">
		SELECT
			question_num
			, question_text
		FROM
			question
	</select>
	
<!-- 			아이디에 면접 결과 저장 -->
	<insert id="startInterview" parameterType="ivo">
		
		<selectKey keyProperty="interview_num" order="BEFORE" resultType="int">
			select interview_seq.nextval from dual
		</selectKey>
	
		INSERT INTO
			interview
			(INTERVIEW_NUM
			,USERID
			,INPUTDATE
		) VALUES (
			#{interview_num}
			,#{userID}
			,SYSDATE)
	</insert>

	
<!-- 	면접 완료 결과 저장 -->
	<insert id="insertInterview" parameterType="rvo">
		INSERT INTO
			interview_result
			(interview_num
			, question_num
			, voicefilename
			, result
		) VALUES (
			#{interview_num}
			, #{question_num}
			, #{voicefilename}
			, #{result}
			)
	</insert>
	
<!-- 	<selcet id="selectInterview"> -->
<!-- 		select  -->
	
<!-- 	</selcet> -->

<!-- 	interviewresult -->
	<select id="resultList" parameterType="ivo" resultType="rvo">
SELECT 
	A.INTERVIEW_NUM,
	A.USERID, 
	A.INPUTDATE,
	B.QUESTION_NUM, 
	B.VOICEFILENAME,
	B.RESULT,
	C.QUESTION_TEXT
FROM 
	INTERVIEW A, 
	INTERVIEW_RESULT B,
	QUESTION C
WHERE 
	A.INTERVIEW_NUM = B.INTERVIEW_NUM AND C.QUESTION_NUM = B.QUESTION_NUM
ORDER BY A.INPUTDATE DESC
	</select>
	
</mapper>
